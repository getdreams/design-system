import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { DefaultLayout } from '~/utils/mdx-layout';
export default DefaultLayout;
import { FormattedText } from '~/components/FormattedText';
import { IntlProvider } from 'react-intl';

# FormattedText

<Description of={FormattedText} />

## Usage

```tsx
import { IntlProvider } from 'react-intl';
import { FormattedText } from '@backlight-dev/dreams.design-system/components/FormattedText';

<IntlProvider locale="en" messages={{ test: 'Render this' }}>
  <FormattedText id="test" />
</IntlProvider>;
```

## Example

<Playground
  scope={{ FormattedText, IntlProvider }}
  code={`
<div className="py-16 w-full max-w-md mx-auto">
  <div className="px-2 py-[1px] bg-white dark:bg-black rounded-2xl">
    <IntlProvider locale="en" messages={{ test: 'Render this' }}>
      <FormattedText id="test" />
    </IntlProvider>
  </div>
</div>
  `}
/>

## Props

<Props of={FormattedText} />
