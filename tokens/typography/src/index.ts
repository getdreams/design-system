import { fontSize, fontWeight } from './tokens.json';

const typography = {
  fontFamily: {
    code: ['Menlo', 'ui-monospace', 'SFMono-Regular', 'Monaco', 'monospace'],
  },
  fontSize,
  fontWeight,
};

export { typography };
